import style = require('ts-style');

export = style.create({
	content: {
		maxWidth: 600,
		paddingLeft: 10,
	    paddingRight: 10,
	    paddingTop: 60,
	   	marginLeft: 'auto',
	    marginRight: 'auto'
	}
});
